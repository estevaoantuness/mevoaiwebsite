{
  "name": "mevo-api",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "cd backend && npx prisma db push && node prisma/seed.js && NODE_ENV=production node server.js",
    "dev": "cd backend && node server.js"
  }
}
